# SDKWork Comments App API Route Specs

This directory declares the component boundary for `sdkwork-router-comments-app-api`.

The route crate is the Rust source catalog for comments app-api routes that materialize into `sdkwork-comments-app-api` and `sdkwork-comments-app-sdk`.
