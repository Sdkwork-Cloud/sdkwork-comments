# SDKWork Comments Storage SQLx Specs

This directory declares the component boundary for `sdkwork_comments_engagement_repository_sqlx`.

The crate owns comments persistence contracts and SQL migrations for thread, comment, reaction, projection, and moderation storage.
