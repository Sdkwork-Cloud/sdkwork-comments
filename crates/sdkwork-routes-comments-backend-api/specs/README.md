# SDKWork Comments Backend API Route Specs

This directory declares the component boundary for `sdkwork-routes-comments-backend-api`.

The route crate is the Rust source catalog for comments backend-api routes that materialize into `sdkwork-comments-backend-api` and `sdkwork-comments-backend-sdk`.
