# SDKWork Comments App SDK Specs

This directory declares the SDK family component boundary for `sdkwork-comments-app-sdk`.

The OpenAPI authority is `sdkwork-comments-app-api`, the fixed prefix is `/app/v3/api`, and TypeScript SDK output is generated by the canonical `sdkgen` entrypoint.
