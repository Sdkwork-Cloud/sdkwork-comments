# SDKWork Comments Backend SDK Specs

This directory declares the SDK family component boundary for `sdkwork-comments-backend-sdk`.

The OpenAPI authority is `sdkwork-comments-backend-api`, the fixed prefix is `/backend/v3/api`, and TypeScript SDK output is generated by the canonical `sdkgen` entrypoint.
