# SDKWork Comments Contracts Specs

This directory declares the component boundary for `@sdkwork/comments-contracts`.

The root SDKWork standards remain authoritative. This package owns comments contract types and local pure helpers only; SDK transport remains generated under `sdks/`.
